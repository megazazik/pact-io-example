# Пример работы с pact.io

Запуск теста

```sh
go test github.com/megazazik/pact-io-example/server
```

Код тестируемого сервера лежит в `./server/server.go`.
Код теста лежит в `./server/server_test.go`.
